//conditionals , control flows (if else) 
// && AND
// || OR


var age = prompt('What is your age?');
if ( (age >= 18) && (age <= 35) ) {
    status = 'target demo';
    console.log(status);
} else {
    status = 'not target demo';
    console.log(status);
}
